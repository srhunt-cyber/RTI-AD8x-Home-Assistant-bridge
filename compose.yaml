services:
  rti-bridge:
    build:
      context: .
    image: rti-ad-series-bridge:1.9.0-beta.1
    restart: unless-stopped
    network_mode: host
    environment:
      RTI_CONFIG: /config/config.yaml
    volumes:
      - ./config.yaml:/config/config.yaml:ro
